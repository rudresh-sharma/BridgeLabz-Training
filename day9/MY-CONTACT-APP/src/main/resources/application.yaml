spring:
  application:
    name: MY-CONTACT-APP

  datasource:
    url: jdbc:h2:file:./data/contactdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console