spring:
  application:
    name: Employee-Pay-Role

  datasource:
    url: jdbc:h2:file:./data/employeepayroll;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  flyway:
    enabled: true

  h2:
    console:
      enabled: true
      path: /h2-console

debug: true