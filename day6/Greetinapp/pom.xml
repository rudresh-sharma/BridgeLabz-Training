<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- =========================================================
         PROJECT COORDINATES
         groupId    : your organisation / reverse domain
         artifactId : unique name for this module
         version    : project version
         packaging  : WAR because we deploy to Tomcat
         ========================================================= -->
    <groupId>com.springmvc</groupId>
    <artifactId>GreetingApp</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>

    <name>GreetingApp</name>
    <description>
        Production-quality Spring MVC 6 Greeting Application demonstrating
        dual view-technology (JSP + Thymeleaf), plain JDBC, MySQL 8,
        and Java 21 deployed on Apache Tomcat 11.
    </description>

    <!-- =========================================================
         GLOBAL PROPERTIES
         These act like constants reused across the POM.
         ========================================================= -->
    <properties>
        <!-- Java version -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>

        <!-- Encoding - always set this to avoid platform differences -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Dependency versions pinned here so they are easy to upgrade -->
        <spring.version>6.1.14</spring.version>
        <thymeleaf.version>3.1.2.RELEASE</thymeleaf.version>
        <thymeleaf-spring6.version>3.1.2.RELEASE</thymeleaf-spring6.version>
        <jakarta.servlet.version>6.0.0</jakarta.servlet.version>
        <jakarta.servlet.jsp.version>3.1.1</jakarta.servlet.jsp.version>
        <jakarta.servlet.jstl.version>3.0.1</jakarta.servlet.jstl.version>
        <mysql.connector.version>8.3.0</mysql.connector.version>
        <slf4j.version>2.0.13</slf4j.version>
        <logback.version>1.5.6</logback.version>
    </properties>

    <dependencies>

        <!-- =======================================================
             1. SPRING MVC (Web layer)
             spring-webmvc brings DispatcherServlet, ViewResolvers,
             HandlerMappings, etc. It transitively pulls spring-core,
             spring-beans, spring-context, spring-web.
             ======================================================= -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!-- =======================================================
             2. SPRING JDBC
             Gives us JdbcTemplate (optional) and DataSource support.
             We use it for DataSource + plain JDBC via DBConnection.
             ======================================================= -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!-- =======================================================
             3. JAKARTA SERVLET API
             Tomcat 11 uses Jakarta EE 10 (jakarta.* namespace).
             scope=provided: Tomcat supplies this at runtime;
             we only need it at compile time.
             ======================================================= -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>${jakarta.servlet.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- =======================================================
             4. JAKARTA JSP API
             Required to compile JSP-related classes.
             scope=provided: Tomcat provides JSP engine at runtime.
             ======================================================= -->
        <dependency>
            <groupId>jakarta.servlet.jsp</groupId>
            <artifactId>jakarta.servlet.jsp-api</artifactId>
            <version>${jakarta.servlet.jsp.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- =======================================================
             5. JSTL (Jakarta Standard Tag Library)
             Provides <c:forEach>, <c:if>, <c:out>, etc. used in JSPs.
             The impl jar is needed at runtime (scope=compile default).
             ======================================================= -->
        <dependency>
            <groupId>jakarta.servlet.jsp.jstl</groupId>
            <artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
            <version>${jakarta.servlet.jstl.version}</version>
        </dependency>
        <!-- JSTL implementation (Glassfish reference impl) -->
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
            <version>${jakarta.servlet.jstl.version}</version>
        </dependency>

        <!-- =======================================================
             6. THYMELEAF CORE
             Template engine that processes HTML files server-side.
             ======================================================= -->
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf</artifactId>
            <version>${thymeleaf.version}</version>
        </dependency>

        <!-- =======================================================
             7. THYMELEAF-SPRING6 INTEGRATION
             Bridges Thymeleaf with Spring MVC 6:
             - SpringTemplateEngine
             - ThymeleafViewResolver
             - Spring Security dialect (optional)
             ======================================================= -->
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf-spring6</artifactId>
            <version>${thymeleaf-spring6.version}</version>
        </dependency>

        <!-- =======================================================
             8. MYSQL CONNECTOR/J
             JDBC driver for MySQL 8. Runtime scope because we
             load it via Class.forName / DataSource at runtime.
             ======================================================= -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql.connector.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- =======================================================
             9. SLF4J API
             Logging facade - code logs via SLF4J interfaces,
             actual implementation is Logback below.
             ======================================================= -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!-- =======================================================
             10. LOGBACK CLASSIC (SLF4J implementation)
             Fast, feature-rich logging backend.
             ======================================================= -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- =======================================================
             11. SPRING TEST (optional, for unit tests)
             ======================================================= -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- =========================================================
         BUILD PLUGINS
         ========================================================= -->
    <build>
        <!-- Final WAR name = GreetingApp.war  (no version suffix) -->
        <finalName>GreetingApp</finalName>

        <plugins>

            <!-- Maven Compiler Plugin - enforces Java 21 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Maven WAR Plugin - packages the project as a WAR.
                 failOnMissingWebXml=false: we use annotation config,
                 so there is no web.xml - don't fail the build. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <warName>GreetingApp</warName>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
